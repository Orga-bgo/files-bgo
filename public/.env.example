# Database Configuration
# These values MUST be set for the application to work
DB_HOST=localhost
DB_NAME=babixgo_files
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# SMTP Configuration (Optional - for email functionality)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_KEY=your_smtp_password

# Site Configuration (Optional)
SITE_URL=https://files.babixgo.de

# Debug Mode (Optional - set to 'true' or '1' to enable debug messages)
# WARNING: Never enable DEBUG_MODE in production!
DEBUG_MODE=false
